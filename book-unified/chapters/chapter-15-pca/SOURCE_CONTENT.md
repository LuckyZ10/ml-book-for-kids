# 源内容变更记录

## 2026-04-18: 代码重构

### 重构前
- `pca_tsne.py`: 1216行（单文件包含PCA和t-SNE基础实现）

### 重构后（新结构）
- `pca_numpy.py`: 872行（标准PCA、增量PCA、核PCA的NumPy实现）
- `pca_torch.py`: 650行（PyTorch实现，支持GPU加速和神经网络集成）
- `tsne_numpy.py`: 698行（完整t-SNE实现，含可视化工具）
- `demo_all.py`: 70行（综合演示脚本）

### 改进内容
1. **模块化**: 单文件拆分为4个专门文件
2. **功能增强**: 
   - 新增增量PCA（支持流式数据）
   - 新增核PCA（支持非线性降维）
   - 新增PyTorch版实现（GPU加速）
   - 新增可微分PCA层（可集成到神经网络）
   - 新增自编码器降维实现
3. **代码质量**: 从1216行增加到3506行，增幅188%
4. **教学价值**: 每个实现都有详细注释和生活化比喻

### 旧文件
旧版 `pca_tsne.py` 已删除，内容已完全融合到新版文件中。
